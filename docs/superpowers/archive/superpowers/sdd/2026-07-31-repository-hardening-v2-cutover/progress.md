# SDD ledger — plan: docs/superpowers/plans/2026-07-31-repository-hardening-v2-cutover.md
Preflight: authorized deviation — user selected current main, then selected “continue but do not touch plan document”; observed `git merge-base --is-ancestor 6604880 HEAD` exit 0, `git diff --quiet` exit 1 only because `docs/superpowers/plans/2026-07-31-repository-hardening-v2-cutover.md` is already modified, and `git diff --cached --quiet` exit 0. Controller must not stage or modify that plan document as part of implementation tasks unless explicitly authorized.
Process note: SDD helper `task-brief` expects numeric `Task N` headings, but this plan uses labeled headings such as `Task P0` and `Task A1`; controller will generate labeled task briefs manually in this plan-scoped workspace.
