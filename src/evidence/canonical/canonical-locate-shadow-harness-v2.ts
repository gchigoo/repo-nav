/**
 * Thin re-export shim removed in F9 — harness lives in testkit only.
 * Kept as a compile-time tombstone so accidental production imports fail loudly.
 */

export type CanonicalLocateShadowHarnessRemovedV2 = never;
